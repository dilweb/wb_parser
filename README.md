# wb_parser

Парсер каталога товаров Wildberries по поисковому запросу. Собирает данные с API и CDN, сохраняет в XLSX.

## Установка

```bash
poetry install
```

## Запуск

```bash
poetry run python wb_parser.py
```

## Настройка

Все параметры в `config.py`:

| Параметр | Описание |
|----------|----------|
| `SEARCH_QUERY` | Поисковый запрос (например, «пальто из натуральной шерсти») |
| `DEST` | Регион доставки (по умолчанию Москва: `-1257786`) |
| `MAX_PAGES` | Максимум страниц поиска |
| `DELAY` | Пауза между запросами в секундах |
| `OUTPUT_ALL` | Путь к полному каталогу |
| `OUTPUT_FILTERED` | Путь к отфильтрованному каталогу |
| `FILTER_MIN_RATING` | Минимальный рейтинг для выборки |
| `FILTER_MAX_PRICE` | Максимальная цена |
| `FILTER_COUNTRY` | Страна производства |

## Выходные данные

Создаются два XLSX-файла:

- **Полный каталог** — все найденные товары
- **Отфильтрованный каталог** — товары с рейтингом >= 4.5, ценой <= 10 000 Руб и страной производства Россия

Колонки: ссылка на товар, артикул, название, цена, описание, ссылки на изображения, характеристики, продавец, ссылка на продавца, размеры, остатки, рейтинг, количество отзывов, страна производства.

## Структура проекта 

```
stud/
├── config.py      # настройки
├── wb_parser.py   # основной скрипт
├── pyproject.toml
└── results/       # папка с результатами
```
